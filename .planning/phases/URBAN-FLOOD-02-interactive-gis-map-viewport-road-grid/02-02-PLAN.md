---
phase: URBAN-FLOOD-02-interactive-gis-map-viewport-road-grid
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/components/GisMap.tsx
  - frontend/src/services/api.ts
autonomous: true
requirements: [MAP-02]
must_haves:
  truths:
    - "OSM Road network GeoJSON from /api/roads/network renders with classification styling"
    - "Low-elevation flood hotspots from /api/roads/hotspots render as pulsing hazard markers"
    - "Clicking or hovering a road segment displays highway category, length, and elevation in a glass popup"
  artifacts:
    - frontend/src/services/api.ts
    - frontend/src/components/GisMap.tsx
---

<objective>
Fetch and overlay the real OpenStreetMap road network and flood hotspots from the FastAPI backend into the Leaflet GisMap, applying custom line stroke styling based on road classification and interactive inspection tooltips.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Create API service layer</name>
  <files>frontend/src/services/api.ts</files>
  <action>Implement fetch functions for /api/roads/network, /api/roads/summary, and /api/roads/hotspots with type-safe interfaces.</action>
  <verify>API calls succeed against proxy or direct backend</verify>
</task>

<task type="auto">
  <name>Task 2: Render styled GeoJSON road layer and hotspot markers in GisMap</name>
  <files>frontend/src/components/GisMap.tsx</files>
  <action>Add GeoJSON layer with custom color weighting (primary: cyan, secondary: teal, residential: slate), interactive hover effects, and amber glowing hotspot markers.</action>
  <verify>Road lines and hotspot points render accurately over CartoDB dark tiles</verify>
</task>
</tasks>
