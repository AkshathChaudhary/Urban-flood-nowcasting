---
phase: URBAN-FLOOD-02-interactive-gis-map-viewport-road-grid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/GisMap.tsx
  - frontend/src/components/CommandCenterView.tsx
autonomous: true
requirements: [MAP-01, MAP-03]
must_haves:
  truths:
    - "Full-screen Leaflet map renders with CartoDB Dark Matter tiles"
    - "Map bounds are properly centered on Mumbai study area (19.06 to 19.08 N, 72.85 to 72.87 E)"
    - "Custom zoom in/out, recenter, and full-screen controls styled with dark glassmorphism"
  artifacts:
    - frontend/src/components/GisMap.tsx
---

<objective>
Mount the full-screen interactive Leaflet GIS map inside `CommandCenterView.tsx`, using high-contrast CartoDB Dark Matter tiles, custom coordinate bounding box framing, and responsive HUD map control buttons.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build GisMap Leaflet component</name>
  <files>frontend/src/components/GisMap.tsx</files>
  <action>Create a robust Leaflet map component using native Leaflet API, Dark Matter tile provider (CartoDB), centered at [19.070, 72.860] with zoom level 14. Add domain bounding box polygon representing the 2km x 2km simulation grid.</action>
  <verify>Map container initializes without DOM leaflet collision errors</verify>
</task>

<task type="auto">
  <name>Task 2: Embed GisMap into CommandCenterView</name>
  <files>frontend/src/components/CommandCenterView.tsx</files>
  <action>Replace placeholder canvas in CommandCenterView with the real GisMap component, connecting layer toggle states and city selection.</action>
  <verify>GisMap renders seamlessly filling the center viewport</verify>
</task>
</tasks>
