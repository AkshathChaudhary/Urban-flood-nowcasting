---
phase: URBAN-FLOOD-04-resilient-emergency-routing-corridor-analytics
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/components/CommandCenterView.tsx
  - frontend/src/components/RoutePanel.tsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03]
must_haves:
  truths:
    - "User can select origin and destination from landmark presets or click on the map"
    - "Vehicle profile selection (Car, Ambulance, Rescue Truck) recalculates route based on clearance"
    - "Route Analytics HUD displays distance (km), travel time (min), max flood depth (m), and risk badge (SAFE/CAUTION/IMPASSABLE)"
  artifacts:
    - frontend/src/components/RoutePanel.tsx
    - frontend/src/components/CommandCenterView.tsx
---

<objective>
Build the 21st.dev inspired Route Analytics HUD panel, allowing dispatchers to select destinations, choose vehicle profiles, and inspect travel metrics and flood hazards along the route.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build RoutePanel component</name>
  <files>frontend/src/components/RoutePanel.tsx</files>
  <action>Create glassmorphic route control card with landmark dropdowns, vehicle clearance pills, 'Find Flood-Safe Route' trigger, and metrics display (ETA, distance, max depth, risk badge).</action>
  <verify>RoutePanel displays calculated metrics cleanly</verify>
</task>

<task type="auto">
  <name>Task 2: Wire RoutePanel into CommandCenterView</name>
  <files>frontend/src/components/CommandCenterView.tsx</files>
  <action>Mount RoutePanel in floating right sidebar or tool palette, sync with GisMap origin/destination pins, and auto-recalculate when timestep or vehicle profile changes.</action>
  <verify>Changing vehicle or timestep updates the active route on the map and panel</verify>
</task>
</tasks>
