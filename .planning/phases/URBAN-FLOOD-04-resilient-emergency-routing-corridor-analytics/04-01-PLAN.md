---
phase: URBAN-FLOOD-04-resilient-emergency-routing-corridor-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/api.ts
  - frontend/src/components/GisMap.tsx
autonomous: true
requirements: [NAV-01, NAV-03]
must_haves:
  truths:
    - "Clicking or selecting origin (A) and destination (B) places interactive Leaflet markers"
    - "Primary route renders as a glowing emerald/cyan polyline with directional arrows or glowing casing"
    - "Avoided flooded road segments render with dashed red line warning styling"
  artifacts:
    - frontend/src/services/api.ts
    - frontend/src/components/GisMap.tsx
---

<objective>
Connect to `/api/route` and `/api/route/destinations`, plot Origin and Destination markers on the Leaflet map, and render the calculated flood-resilient route geometry with glowing stroke effects.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add routing contracts and endpoints to api.ts</name>
  <files>frontend/src/services/api.ts</files>
  <action>Implement calculateFloodRoute(request: RouteRequest) and fetchDestinationsCatalog() with full TypeScript types.</action>
  <verify>POST /api/route returns route_found, primary_route, and geojson</verify>
</task>

<task type="auto">
  <name>Task 2: Render route line and origin/destination pins in GisMap</name>
  <files>frontend/src/components/GisMap.tsx</files>
  <action>Add route GeoJSON layer to Leaflet map, origin (A: Emerald) and destination (B: Crimson) markers, and zoom-to-fit route bounds.</action>
  <verify>GisMap renders the primary route path between A and B</verify>
</task>
</tasks>
