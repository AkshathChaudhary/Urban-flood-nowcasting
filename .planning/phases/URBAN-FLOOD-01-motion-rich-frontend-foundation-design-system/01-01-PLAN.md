---
phase: URBAN-FLOOD-01-motion-rich-frontend-foundation-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tailwind.config.js
  - frontend/src/index.css
  - frontend/src/App.tsx
autonomous: true
requirements: [FE-01, MAP-01]
must_haves:
  truths:
    - "Vite + React + TypeScript application builds cleanly without errors"
    - "Tailwind CSS is configured with tokens from design-system/urban-flood-command-center/MASTER.md"
    - "Inter typography, dark surface background (#020617), and glassmorphism styling are active"
  artifacts:
    - frontend/package.json
    - frontend/tailwind.config.js
    - frontend/src/index.css
---

<objective>
Scaffold the frontend workspace using Vite + React 18 + TypeScript, configure Tailwind CSS with the design system tokens from `design-system/urban-flood-command-center/MASTER.md`, install 21st.dev UI dependencies (`lucide-react`, `framer-motion`, `clsx`, `tailwind-merge`, `leaflet`), and establish the root styling and application layout.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Initialize Vite React TypeScript project</name>
  <files>frontend/package.json, frontend/vite.config.ts</files>
  <action>Create frontend directory and initialize Vite + React + TypeScript with non-interactive flags, configure port 5173 with proxy to backend port 8000.</action>
  <verify>npm run build in frontend/ succeeds</verify>
</task>

<task type="auto">
  <name>Task 2: Install UI dependencies and configure Tailwind CSS</name>
  <files>frontend/tailwind.config.js, frontend/src/index.css</files>
  <action>Install tailwindcss, postcss, autoprefixer, framer-motion, lucide-react, clsx, tailwind-merge, leaflet, @types/leaflet. Wire MASTER.md tokens into tailwind theme colors, shadows, and spacing scales.</action>
  <verify>frontend/src/index.css imports Google Font Inter and defines root color variables</verify>
</task>
</tasks>
