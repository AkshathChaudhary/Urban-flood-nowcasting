---
phase: URBAN-FLOOD-03-hydrodynamic-2d-flood-inundation-radar-scrubber
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/components/CommandCenterView.tsx
  - frontend/src/components/GisMap.tsx
autonomous: true
requirements: [SIM-01, SIM-03]
must_haves:
  truths:
    - "Clicking anywhere on the map triggers point depth query and displays water depth progression across all forecast horizons"
    - "Play button advances the timeline automatically with loop support"
    - "HUD displays real-time peak water depth, flooded cell count, and precipitation dBZ"
  artifacts:
    - frontend/src/components/CommandCenterView.tsx
---

<objective>
Implement timeline playback loop, point depth inspection modal/popup, and live HUD metrics synchronization for Phase 3.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add Point Depth Inspector popup in GisMap</name>
  <files>frontend/src/components/GisMap.tsx</files>
  <action>Attach map click listener that fetches queryPointDepth(lat, lon), displaying coordinates, elevation, water depth at current horizon, and vehicle hazard badge.</action>
  <verify>Clicking on the map shows accurate point depth popup</verify>
</task>

<task type="auto">
  <name>Task 2: Implement animated timeline scrubber loop & HUD telemetry in CommandCenterView</name>
  <files>frontend/src/components/CommandCenterView.tsx</files>
  <action>Create auto-advance interval when isPlaying is true (stepping through 0, 30, 60, 90, 120, 180 min). Update top HUD bar with active horizon stats (peak depth, surface water volume).</action>
  <verify>Play button animates scrubber forward and updates map raster</verify>
</task>
</tasks>
