---
phase: URBAN-FLOOD-03-hydrodynamic-2d-flood-inundation-radar-scrubber
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/api.ts
  - frontend/src/components/GisMap.tsx
autonomous: true
requirements: [SIM-01, SIM-02]
must_haves:
  truths:
    - "Canvas-based 2D water depth raster overlay renders on top of the Leaflet map within the 2km x 2km bounds"
    - "Color coding accurately maps water depth: <0.05m transparent/shallow, 0.05-0.15m teal, 0.15-0.30m amber, >0.30m red"
    - "Raster updates smoothly when scrubbing the timeline between 0 and 180 minutes"
  artifacts:
    - frontend/src/services/api.ts
    - frontend/src/components/GisMap.tsx
---

<objective>
Build high-performance 2D hydrodynamic flood depth raster generation using an offscreen canvas and Leaflet ImageOverlay, connected to `/api/flood-forecast/{minutes}`.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add flood forecast endpoints to api.ts</name>
  <files>frontend/src/services/api.ts</files>
  <action>Implement fetchFloodOverview(), fetchFloodGrid(minutes), and queryPointDepth(lat, lon) functions with cache support.</action>
  <verify>API calls return 200x200 depth grid arrays and summary metrics</verify>
</task>

<task type="auto">
  <name>Task 2: Render dynamic flood depth raster in GisMap</name>
  <files>frontend/src/components/GisMap.tsx</files>
  <action>Generate ImageData from depth_grid, render to offscreen canvas with color threshold gradient, and update Leaflet L.imageOverlay with smooth opacity blending.</action>
  <verify>ImageOverlay updates seamlessly when timestep prop changes</verify>
</task>
</tasks>
